site_name: arxiv2025_misleading_visualizations
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2025_misleading_visualizations
      handlers:
        python:
          options:
            docstring_style: numpy